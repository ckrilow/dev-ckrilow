// General resource configuration applicable to all profiles

process {
    // error strategy
    errorStrategy = 'retry'
    maxRetries = 3

    // basic resources
    cpus = 1
    //time = { 20.m * task.attempt }

    // process-specific resources
    withName: merge_10x_samples {
        memory = { 15.GB * task.attempt }
    }
    withName: normalize_seurat_obj {
        memory = { 50.GB * task.attempt }
    }
} // end process

timeline {
    enabled = true
}
