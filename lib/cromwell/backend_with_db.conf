include "backend.conf"

database {
  profile = "slick.jdbc.MySQLProfile$"
  db {
    driver = "com.mysql.cj.jdbc.Driver"
    url = "jdbc:mysql://localhost/cromwell_db?useSSL=false&rewriteBatchedStatements=true"
    user = "cromwell"
    password = "cromwell"
    connectionTimeout = 5000
  }
}


call-caching {
  #enabled = true
  invalidate-bad-cache-results = true
}
